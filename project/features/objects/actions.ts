class Objects{

    id: number = 0;
    name: string = "";
    age: number = 0;
    items: string[] = [];

    constructor(id: number, name: string, age: number, items: string[]){

        this.id = id;
    }
  
}

const obj1 = new Objects(1, "s", 1, ["ds", "D"]);

export default function getObjects(){

    return
}